# 在线化就绪度评估：从离线单帧到连续视频流

> 回答两个问题：(1) 现在离「在线 3D 实例感知 + 语义地图构建」还有多远；
> (2) 从离线单帧升级到连续视频流上的在线感知与时序融合，具体要补什么。
>
> 结论：**算法骨架已经是因果的，缺的是编排、实时性和位姿。真正的分水岭是 SLAM。**

---

## 0. 一句话结论

现在的状态是 **「因果算法 + 离线编排 + 数据集 GT 位姿」**。

- 检测→分割→3D 提升**确实逐帧在线跑**（进程内循环，模型只加载一次）；
- 跨帧关联算法**本身严格因果**（只看历史滑动窗口，不看未来帧）；
- 但**关联与融合在序列末尾统一批处理**，实例语义嵌入和 3D 实例点云都是**事后重放**出来的；
- **位姿 100% 来自数据集 `traj.txt`**，代码库里没有任何里程计 / 闭环 / 位姿图。

所以严格讲：现在跑的是「按帧顺序处理离线数据」，不是「连续视频流上的在线感知」。
差距不在算法思路，而在工程编排 + 实时性 + 位姿自给。

---

## 0.4 Demo A 需求定义（明确版）与逐条距离

需求方明确的 Demo A 定义：

> 输入**连续 8 个场景的 RGB + Depth + 相机位姿**，实现 **3D 实例感知 + 语义地图构建**；
> 新场景输入进来也保持好效果；实时性好。

**关键：位姿是输入，不是要我们估计的。** 因此 §5 的 **S3（接 SLAM）不在 Demo A 范围内**——
这一条把整体距离从「1–2 月+」缩短到「周级」。逐条对照：

| 需求条目 | 现状 | 距离 | 阻塞项 |
|---|---|---|---|
| 输入 RGB-D + 位姿 | 已支持（`ReplicaSequence` 读 `traj.txt`） | **0** | — |
| 3D 实例感知 | 有，L1 级（单轨率 0.706） | 0（质量另计） | 掩码质量 AP@0.50 |
| 语义地图构建 | 有（体素地图 + CLIP 可查询实例） | 0（但语义嵌入是离线的） | S1 |
| **新场景泛化** | **⚠ 最大风险，见 §0.6** | **未知，正在测** | 类别封闭 |
| 实时性好 | 191 ms/帧 = 5.2 fps（0.17× 实时） | 2–4 周 | S2 |
| 真在线增量 | 算法因果，编排离线 | 1–2 周 | S1 |

**结论**：位姿外给之后，剩下的硬骨头是 **「新场景泛化」和「实时性」**，
而泛化这一条**比在线化更难**，且此前被 6 类封闭词表掩盖了。

---

## 0.6 ⚠ 泛化风险：检测词表只有 6 类，场景里却有 56 类

```python
# tools/run_multiscene_eval.py
DEFAULT_PROMPTS = ["computer monitor", "chair", "desk", "trash can", "door", "sofa"]
GT_CLASSES = [bin basket tissue-paper chair sofa stool armchair couch door
              table desk desk-organizer tv-screen tablet monitor]   # 15 类
```

对 8 个场景的 `info_semantic.json` 做类别审计的结果：

| 指标 | 数值 |
|---|---|
| 8 场景出现的语义类别总数 | **56** |
| 不在当前 15 类 GT 白名单内的 | **43** |
| 单场景类别数（office_3 / room_0） | 100 / 92 个标注物体 |

高频但我们**根本不去检**的类别（出现次数）：
`lamp` 36、`cushion` 18、`table` 16、`book` 10、`pillow` 9、`bottle` 6、
`indoor-plant` 5、`clock` 4、`shelf`、`cabinet`、`nightstand` …

**后果有两层，第二层更严重：**

1. **能力缺口**：新场景若以灯、书架、床、柜、植物为主，系统直接哑火——
   检测器拿到的文本提示里没有这些东西。
2. **指标测不出缺口**：GT 评估白名单同样只有 15 类，检测端漏掉的 43 类
   **完全不进入指标**。所以 headline 的 AP@0.50 = 0.519 是在一个小封闭世界里测的，
   **不等于「换个场景也好」**。这是继 office_0/room_2 方差之后，指标虚高的第二个来源。

**两条解法（二选一，代价不同）：**

| 方案 | 做法 | 代价 | 风险 |
|---|---|---|---|
| **A 扩词表** | prompt 扩到 20–50 类 | 检测变慢（DINO text encoder 随词数增长）+ 误检上升 | 治标，仍受词表上限约束 |
| **B 换架构** | 类别无关 proposal（SAM 自动掩码 / CropFormer）+ CLIP 后验打标签 | 要动 2D 前端，工作量大 | 治本，才是真开放词汇 |

### 0.6.1 实测：20 类词表 vs 6 类词表（200 帧，同 GT，同权重）

命令见 §7。结果：

| 场景 | 词表 | AP@.25 | AP@.50 | P@.50 | R@.50 | GT 实例 | 预测 |
|---|---|---|---|---|---|---|---|
| office_0 | 6 类 | 0.583 | 0.334 | 0.722 | 0.503 | 1167 | 813 |
| office_0 | **20 类** | **0.624** | **0.397** | 0.533 | **0.580** | 1442 | 1567 |
| room_2 | 6 类 | 0.889 | **0.878** | 0.922 | 0.883 | 1065 | 1019 |
| room_2 | **20 类** | 0.503 | **0.389** | 0.786 | 0.511 | 2968 | 1930 |

延迟：191 → 215 ms/帧（+13%），检测 120 ms 基本不变（DINO-tiny 的 text encoder 不是瓶颈）。

**怎么读这张表（两个场景符号相反，原因必须讲清）：**

1. **office_0 上升**：GT 白名单只从 1167 涨到 1442（+275），而新增词表让它**真正检到了更多东西**，
   召回 0.503 → 0.580，AP@0.50 0.334 → 0.397。这是实打实的能力增益。
2. **room_2 暴跌**：GT 白名单从 1065 涨到 **2968（+1900）**——灯、书、枕头、植物、地毯
   这些原本被排除在评估之外的物体**终于被算进来了**，系统检不到它们，召回 0.883 → 0.511。
   **这不是系统变差，是把原本藏起来的缺口暴露了。**
3. **所以 room_2 那个 0.878 是被严重高估的数字**——它只在 15 类封闭世界里成立。
   按场景真实类别分布衡量，实际覆盖能力是 **0.39 这个量级**。
4. **误检代价真实存在**：office_0 precision 0.722 → **0.533**，预测数 813 → 1567（近翻倍）。
   对语义地图来说这是观感问题——**地图里大约每两个实例就有一个是假的**。

**结论**：扩词表**可行且便宜**（延迟只 +13%，office_0 上 AP 还涨了），
但它同时揭开了底：当前系统的真实开放词汇覆盖能力约 **AP@0.50 ≈ 0.39**，
而 headline 的 0.519 是在封闭小世界里测的。要达到"新场景也好"，
光扩词表不够——得同时压住误检（换更准的分割器 / 类别无关 proposal + CLIP 后验）。

### 0.6.2 顺带修掉一个真实的泛化崩溃

扩展词表后首次出现稳定崩溃：

```
ValueError: 实例掩码中没有有效深度      # src/geometry/instance_lifting.py
```

原因：新增类别（window / picture 等）会检到**窗户外的远景、玻璃、反光面**，
这些区域深度无效，3D 提升直接抛异常，**整条序列中断**。
6 类词表下永远不会遇到——**这就是"泛化"最典型的失败模式：新类别带来新崩溃。**

修复：`tools/run_sequence_efficient.py` 主循环改为逐实例 try/except，
丢弃无有效深度的观测，并保持 `detections / masks / geometries` 三者对齐，
末尾打印丢弃计数。不影响 6 类 baseline（该分支从未触发）。

---

## 0.5 Demo A 到底要什么：三种定义，三种距离

上一版评估容易让人误解成「Demo A 还早」。其实取决于 Demo A 怎么定义——
**这三种定义的距离差着一个数量级**，必须分开谈：

| Demo A 的定义 | 还需要什么 | 距离 | 备注 |
|---|---|---|---|
| **A1 效果演示**：语义地图 + 文本查询 + 第一视角视频，用标准 RGB-D 序列（GT 位姿） | 已基本具备，剩下是**换场景 + 打磨观感** | **几天** | 社区通行做法：OVI-MAP / ConceptFusion 一类工作也用 GT 位姿，**没有 SLAM 不等于不合格** |
| **A2 真在线增量演示**：边跑边建图、边查询，第 t 帧只依赖 0..t，增量结果≈批聚合 | **S1 编排改造**（仍是 GT 位姿） | **1–2 周** | 算法已是因果的，纯粹是把调用挪进逐帧循环 + 嵌入/点云改增量 |
| **A3 自给位姿的真机在线**：不依赖任何 GT 轨迹，自己估位姿 + 闭环 | **S3 接 SLAM** 及其后的判据重调 | **1–2 月** | 这才是「上真机」的门槛，但**未必属于 Demo A 的必需要求** |

**关键纠正**：真正让现在这版 Demo A 站不住的，**不是"在线"，是"质量"**——
AP@0.50 只有 0.240（office_0 那段）/ 0.519（8 场景宏平均），掩码边缘肉眼可见不准。
S1 只需 1–2 周就能解决"在线"的名义问题；掩码质量才是要花大力气的地方，
且换检测器已验证无效（§5.5），得动分割器。

**建议路径**：先做 A1→A2（几天 + 1–2 周），让 Demo A 名副其实；
S3 单列，按是否需要上真机再决定投入。对标论文普遍止步于 A2。

---

## 1. 时序融合的四级划分：我们在 L1，目标是 L3

| 级别 | 能力 | 状态 | 证据 |
|---|---|---|---|
| **L0** | 单帧独立检测+分割，帧间无身份 | 早已超过 | — |
| **L1** | 跨帧关联，同一物体有稳定全局 ID | **已达成（质量中等）** | 单轨率 0.706、纯轨率 0.887、碎片率 1.363、标签一致 0.984 |
| **L2** | 多观测**融合**：把 N 次观测合成一个更准的 3D 实例 + 更鲁棒的语义 | **部分达成，但是离线的** | 实例点云靠 `replay_instance_fusion` 事后重放；CLIP 嵌入靠 `extract_instance_embeddings` 遍历全序列后面积加权平均 |
| **L3** | **在线增量融合** + 闭环后轨道合并 + 地图老化 | 未开始 | 无闭环；已分裂的两条轨道不会在线合并 |

碎片率 1.363 说明：同一物体平均被切成 1.36 条轨道。L1 做到了「大部分时候 ID 稳定」，
但还没做到「一条轨道 = 一个物体」。

---

## 2. 已经真正在线的部分（不用重写）

| 组件 | 位置 | 在线性 |
|---|---|---|
| 逐帧 检测→SAM2→3D 提升 | `tools/run_sequence_efficient.py` | ✅ 进程内循环，模型只加载一次（旧版逐帧 spawn 子进程 30.7s/帧，90% 是脚手架开销，已修） |
| 因果跨帧关联 | `src/mapping/geometric_instance_tracker.py` | ✅ `update(obs, frame_index)`；体素滑动窗口 `voxel_window_size=20`；`last_processed_frame` 保护；**不使用未来帧** |
| 单帧内过分割合并 | `merge_frame_instances()` | ✅ 用体素覆盖率在**当前帧内**合并重复观测，并重新做 3D 提升 |
| 增量体素地图 | `src/mapping/online_voxel_map.py` | ✅ `update()` 增量写入，`to_numpy()` 按需导出 |

关联判据从 IoU 换成**体素覆盖率**是关键改进：同一物体覆盖率 ~0.7+、不同物体 ~0，间隔干净，
修掉了「视角变化导致质心漂移把同一物体切成两条轨道」的老问题。

---

## 3. 假在线：目前仍是离线批处理的部分

| 环节 | 现状 | 为什么不算在线 |
|---|---|---|
| **跨帧关联** | 序列末尾统一跑一次 | `run_sequence_efficient` 文档明写「关联 / 预览 / 融合只在末尾对整条序列跑一次」。算法因果 ≠ 编排在线 |
| **实例 CLIP 嵌入** | 遍历全序列收集裁剪图 → 批量编码 → **面积加权平均** → L2 归一化 | 必须等所有帧跑完才能算出实例语义，无法「边走边查」 |
| **实例 3D 融合** | `replay_instance_fusion` 按已有记录重放 | 事后脚本，明确声明「不重新推理、不估计位姿」 |
| **评估** | `evaluate_against_gt` 序列末尾批量跑 | 不影响在线性，但说明整条链路是批处理思维 |

---

## 4. 根本性缺口（这才是"多远"的尺子）

### 4.1 位姿完全依赖数据集 GT —— 最大缺口

```python
# src/datasets/replica_sequence.py
self.pose_path = self.scene_directory / "traj.txt"
self.camera_to_world = load_camera_poses(self.pose_path)
```

全库检索 `loop_closure` / `pose_graph` / `relocal` / `odometry` / `slam` / `ORB` / `ICP`
**零命中**（"nice-slam" 只是数据集来源标记，不是我们实现了 SLAM）。

这不只是"少个模块"：关联判据（体素覆盖率）**直接依赖位姿精度**。
位姿一漂，同一物体的跨帧体素重合度就塌，覆盖率判据失效，碎片率会显著恶化。
我们目前报的 0.706 单轨率是**在 GT 位姿下的上界**，真实 SLAM 位姿下必然更低。

### 4.2 实时性差约 6 倍

| 场景 | 均值 | P95 | FPS | 检测占比 |
|---|---|---|---|---|
| office_0 | 191.1 ms | 238.7 | 5.23 | 132.6 ms |
| office_1 | 156.0 ms | 199.6 | 6.41 | 130.4 ms |
| office_3 | 218.1 ms | 254.1 | 4.58 | 128.8 ms |

检测占 ~70%。按 30fps 视频流算，实时率仅 **0.17×**。
且这是 RTX 3090；Demo B 目标平台 RK3588 另算。

### 4.3 没有检测/跟踪分工

每帧都完整跑一遍 Grounding DINO（130ms）。在线系统的常规做法是
**低频检测 + 高频传播**（每 N 帧检测一次，中间帧用光流/掩码 warp/SAM2 传播），
这一层目前完全没有。

### 4.4 无闭环、无地图边界管理

- 已分裂的两条轨道不会在线合并（离线 `plan_merges` 只在单帧内合并）；
- 体素地图只增不减，长时间运行内存/算力单调增长；
- 无实例重识别（物体离开视野再回来会被判为新实例）。

---

## 5. 分期路线与工作量估计

| 阶段 | 内容 | 难度 | 估计 | 依赖 |
|---|---|---|---|---|
| **S1：把因果算法接成真在线** | tracker.update 挪进逐帧循环；CLIP 嵌入改增量 running average；实例点云增量累积；暴露「当前已发现实例」查询接口 | 低（算法现成，改编排） | **1–2 周** | 无 |
| **S2：追实时** | 检测降频 + SAM2 掩码传播；检测/分割异步流水线；目标 10–15 fps | 中（工程优化） | **2–4 周** | S1 |
| **S3：接 SLAM** | 接入 ORB-SLAM3 / Nice-SLAM 提供位姿；重测位姿误差对覆盖率判据的冲击；必要时放宽门限/改用鲁棒判据 | **高（最大不确定性）** | **1–2 月** | S1 |
| **S4：长时序鲁棒** | 闭环后轨道合并、地图老化/遗忘、实例重识别 | 高 | 1–2 月 | S3 |

**分水岭是 S3**。S1/S2 只是把已有的东西接对、跑快；
S3 决定这套东西能不能离开数据集、上真机。在 S3 完成前，
任何「在线」指标都是在 GT 轨迹上测的乐观值。

---

## 7. 复现：泛化实验

```bash
# 6 类 baseline（既有结果）
python -m tools.run_multiscene_eval --all --real-only --skip-render \
    --output-root outputs/multiscene_eval_v2

# 20 类扩展词表（office_0 + room_2）
python -m tools.run_multiscene_eval --scenes office_0 room_2 --skip-render \
    --output-root outputs/ov_ab_p20 --dino-model checkpoints/grounding-dino-tiny \
    --classes 'computer monitor' chair desk 'trash can' door sofa table bed lamp \
              shelf cabinet book bottle cushion pillow 'potted plant' television \
              window rug clock \
    --gt-classes bin basket tissue-paper chair sofa stool armchair couch door \
              table desk desk-organizer tv-screen tablet monitor bed lamp shelf \
              cabinet book bottle cushion pillow indoor-plant window rug clock \
              nightstand bench vase plate bowl box picture
```

注意两组**评估白名单不同**（15 类 vs 34 类），这不是严格同口径对比——
差异本身就是实验要暴露的东西：白名单一放开，GT 实例数 room_2 从 1065 涨到 2968。

---

## 6. 建议的验收口径

现在说"在线"容易被质疑，建议改用可验证的三个数字说话：

1. **实时率**：`处理帧率 / 采集帧率`（当前 0.17×，目标 ≥1.0×）；
2. **因果性**：断言第 t 帧的输出只依赖 0..t 帧（tracker 已满足，编排需改造）；
3. **在线一致性**：在线增量算出的实例嵌入 / 点云，与离线批聚合结果的偏差
   （余弦 / Chamfer），用来证明"增量 ≈ 全量"。

没有这三条，所谓"在线感知"只是把离线数据按帧顺序喂进去。
